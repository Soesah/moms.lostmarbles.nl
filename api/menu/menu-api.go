package menu

import "net/http"

func UploadMenu(r *http.Request) {

	// parse the menu
	// find the date
	// create ingredients or find them
	// create meals (with refs to the ingredients) or find them

	// create the menu with refs to the meals
}
